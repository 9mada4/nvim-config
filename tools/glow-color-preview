#!/usr/bin/env bash
set -euo pipefail

# Force a pseudo-TTY so glow emits ANSI colors even when called by glow.nvim.
env -u NO_COLOR TERM=xterm-256color COLORTERM=truecolor script -q /dev/null glow "$@"
